/* IDs of the sections to define the scroll behavior. This was previously defined in navbar.component.tsx but it was causing a compile error */
export enum SectionIDs {
  INTRODUCTION = 'apresentacao',
  PROCEDURES = 'procedimentos',
  PATHOLOGIES = 'patologias',
  REVIEWS = 'avaliacoes',
  FAQ = 'duvidas'
}
